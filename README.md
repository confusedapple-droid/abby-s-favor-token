# Favor Cards

Static site hosted on Cloudflare Pages + optional Cloudflare Worker backend.

## Deploy Static Site
- Connect GitHub repo to Cloudflare Pages
- Build command: (leave empty)
- Output directory: public

## Deploy Worker
cd worker
wrangler deploy
